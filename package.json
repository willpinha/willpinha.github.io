{
  "name": "my-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "eleventy --serve",
    "resume": "uv run --project resume rendercv render resume/resume.yaml",
    "build": "npm run clean && npm run resume && GITHUB_TOKEN=\"${GITHUB_TOKEN:-$(gh auth token)}\" eleventy",
    "clean": "rm -rf dist resume/output",
    "test": "vitest run"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@11ty/eleventy-plugin-rss": "^3.0.0",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.2",
    "vitest": "^5.0.0"
  }
}
